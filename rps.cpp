#include <iostream>
// #include "lib/game.h"
#include "lib/player.h"

using namespace std;

main()
{
  Player human("Human", true);
  Player bot("Bot", false);


  // Game game;
  // Game.players = {human, bot};

  return 0;
}